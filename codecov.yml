coverage:
  status:
    project:
      default:
        target: 93%    # the required coverage value
        threshold: 2%  # the leniency in hitting the target
    patch:
      default:
        target: 50%    # lower for patches since cross-version code (Spark 3/4 shims) is only covered by one CI job
